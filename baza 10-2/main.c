#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, v[100], i;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d", &v[i]);
    }
    for(i=0;i<n;i++)
    {

    }
    return 0;
}
