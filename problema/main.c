#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main()
{
    int x;
    double y
    scanf("%d %f", &x);
    printf("Suma: %d %d %d",(x*(x+1))/2, (x*(x+1)*(2*x+1))/6, pow(x*(x+1)/2,2)) ;
    return 0;
}
